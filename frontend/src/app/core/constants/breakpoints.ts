// TODO: look for tailwind breakpoints and rewrite these, maybe will add min and max for the md or just rewrite the tw breakpoints here
export const Breakpoints = {
  md: '(min-width: 768px)',
  sm: '(max-width: 640px)',
  xs: '(max-width: 435px)',
} as const;
