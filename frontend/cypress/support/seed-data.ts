export const EXISTENT_USER = { email: 'abdelkhalek@gmail.com' };
// Assuming standard slug generation, this must match the first created store in `creates two stores` spec.
export const STORE_ONE_SLUG = 'store-one';
